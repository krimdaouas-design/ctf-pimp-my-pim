name: Trigger CTF
on:
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Set command
        run: |
          # On peut directement écrire dans $GITHUB_ENV pour définir COMMAND
          echo 'COMMAND=gh status' >> $GITHUB_ENV
      - name: Trigger vulnerable workflow
        run: |
          # On peut utiliser l'API pour créer une issue ou déclencher l'autre workflow
          echo "Créer l'issue ou appeler le workflow vulnérable ici"
